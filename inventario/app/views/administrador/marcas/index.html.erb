<h1>Listing marcas</h1>

<table>
  <thead>
    <tr>
      <th>Nombre marca</th>
      <th></th>
      <th></th>
      <th></th>
    </tr>
  </thead>

  <tbody>
    <% @marcas.each do |marca| %>
      <tr>
        <td><%= marca.nombre_marca %></td>
        <td><%= link_to 'Show', administrador_marca_path(marca) %></td>
        <td><%= link_to 'Edit', edit_administrador_marca_path(marca) %></td>
        <td><%= link_to 'Destroy', [:administrador,marca], method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Marca', new_administrador_marca_path %>
